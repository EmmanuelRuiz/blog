<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="utf-8">
        <title> {% block title %} Blog | CreaLab {% endblock %} </title>
        <script src="{{ asset('assets/js/jquery.min.js')}}"></script>
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css')}}">
        <script src="{{ asset('assets/js/bootstrap.min.js')}}"></script>        
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a  class="navbar-brand" href="{{path('blog_homepage')}}">CreaLab</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">                                  
                        {% if app.user == null %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('login') }}">login</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <p>Bienvenido {{app.user.name}} {{app.user.surname}} </p>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('blog_index_tag') }}">Etiquetas</a>
                            </li>
                           
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('logout') }}">Salir</a>
                            </li>
                           
                        {% endif %}
                
                    </ul>
                </div>
            </nav>
        </header>
        <section id="content">
            {% block content %} Contenido por defecto {% endblock %}
        </section>
        <footer>
            Crealab &copy;
        </footer>

    </body>

</html>